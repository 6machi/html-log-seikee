# CoCログ縦書きメーカー 軽量版 GitHub ver6

A5安定基準。縦書きの列分割を、単純な文字数ではなくブラウザ上の実測に近い「箱組み自動分割」へ変更した版です。

## 追加
- 会話文・地の文を固定枠に入れて、はみ出す直前で自動的に次列へ送る処理
- ステータス表示を「箱組み自動分割」に変更
- ver3の縦書きプレビュー編集機能は維持


## ver6
A5の箱組みを改良し、セリフを39文字相当まで伸ばしながら、箱からはみ出す前に自動で次列へ送る処理を追加しました。
